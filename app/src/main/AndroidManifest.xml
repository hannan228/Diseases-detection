<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jav.app.diseasedetection">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiseaseDetection">
        <activity android:name=".Resultt"></activity>
        <activity android:name=".SplashScreen" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Symptom4" />
        <activity android:name=".Diarrhoea" />
        <activity android:name=".Fever" />
        <activity android:name=".Pain" />
        <activity android:name=".AboutPatient" />
        <activity android:name=".PatientInfo" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>